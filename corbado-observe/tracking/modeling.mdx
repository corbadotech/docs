---
title: "Model Your Authentication Journeys"
description: "How to map your login, sign-up, recovery and enrollment journeys onto Corbado Observe flows, decisions and subflows before you write tracking code."
sidebarTitle: "Modeling"
---

Custom events are only the transport. A backend classifier reads each session's event stream and produces the **flows**, **decisions** and **subflow attempts** that every dashboard is built on. Raw events remain visible in the console for debugging a single user, but the classified output is what carries the value.

The classifier is not visible from your integration, so this page describes the rules it applies and a method for mapping your journeys onto them. Reason about every event you emit from the classifier's perspective, not from how the raw series looks.

## 1. Three passes

Work the mapping in three passes, global concerns first. Mistakes in early passes cost the most. Later passes are local problems with a small blast radius that you can solve pragmatically.

<Steps>
  <Step title="Flow boundaries">
    For every flow, find the single best signal for when it starts, when it finishes successfully and, where your product has one, when it is skipped. Do this for all flows, top-level and nested, before anything else. The result of this pass is an exact definition of what is tracked. Precision matters most here: a `flowFinished()` without a matching open flow invalidates the whole session's classification.
  </Step>
  <Step title="Decision structure">
    Assign every screen of the journey to a decision name, then find the simplest way to determine the selectable option set per screen. Look for one mechanism that yields options globally if your app offers it, for example a server response that already lists the rendered choices.
  </Step>
  <Step title="Subflows">
    Fill in one auth method attempt at a time. What matters is creating the operation helper at the right moment, then mapping your app's signals onto the helper's steps.
  </Step>
</Steps>

The taxonomy is not a to-do list. Not every detail must be modeled. Prefer a clean, documented lossy mapping over a contorted complete one.

## 2. Flow boundaries

**One declared opener per flow.** `flowStarted()` fires at the flow's own entry screens only. Every other handler drops its signals when no flow is open instead of opening one lazily. Never re-announce a parent flow from a nested page "to make sure it is open": the classifier reads a repeated start of an outer flow as a restart and closes everything nested under it as incomplete.

**Finish only on success or explicit skip.** Do not model non-completion on the client. Incompleteness is classified from the absence of a `flowFinished()`. Skipping is the one exception, because it is semantically different from abandoning: send `flowFinished({ flowName, explicitOutcome: "skipped" })` for "continue as guest" or "not now", carrying the user reference whenever identity is already known.

**Nesting versus chaining.** Only `login` and `signup` can contain nested flows. A flow that itself establishes the session, such as a sign-up or a recovery started from the login page, nests inside `login`. When the nested flow's own terminal fires, complete the parent with `flowAutoFinished()`. A flow that runs after the session already exists, typically a passkey enrollment prompted after login, is chained: a sibling flow started after the login finished, never nested.

Events attribute to the innermost open flow. When the user leaves a nested flow without finishing it, close it explicitly with `explicitOutcome: "skipped"`, for example when entering sign-up abandons an open recovery. That records the right outcome and keeps the parent's subsequent events out of the nested flow.

**Ambiguous entry.** On a combined login and sign-up form, start with `flowNames: ["login", "signup"]` and resolve with `flowDecided()` once your backend knows whether the identifier exists.

See [Flows](/corbado-observe/tracking/flows) for the event reference.

## 3. Decision structure

A decision name is a **checkpoint** of the journey: a semantic unit that takes a successful auth method to pass and navigation to leave. Several screens usually map to one name (progressive disclosure, explanatory screens, switching between verification methods). Typical names are `pre-identifier` for everything before the identifier is submitted, `post-identifier` for the methods shown after it and `2fa`.

A screen's option set mixes two kinds of options:

| Kind | Examples | How it resolves |
| --- | --- | --- |
| **Method options** | password, passkey, email OTP, social | The subflow that follows resolves the decision in classification. Never finish these explicitly. |
| **Navigational options** | back, change identifier, switch to sign-up, other methods, forgot password | Finish explicitly with `explicitDecisionValue` the moment the choice is made, then start the next screen's decision. |

Realistic logins have many navigational options. They are the adaptive, per-application part of the model, not an edge case. Name them for reuse across decisions.

A screen can lead into the **same subflow through several controls**: two buttons that run the same ceremony, a primary tile plus an "other methods" list, a prefilled versus a typed identifier. That is one option and one subflow, because only one option string can resolve. Express the difference through an explicit spec type where the taxonomy has one, otherwise carry it as a tag or accept the loss and document it.

See [Decisions](/corbado-observe/tracking/decisions) for presentation rules and the option strings each subflow resolves.

## 4. Subflows

A subflow is one auth method attempt. Creating its operation helper emits the attempt's start, so **when** you create the helper is the modeling question:

- **Input-bound methods** (password, OTP, identifier, provide-data) start when the input renders. The field itself is the attempt surface, and the helper captures interaction on it.
- **Action-bound methods** (passkey button, social button, app confirmation) start on the action, not when the button becomes visible. A helper created for a button nobody pressed produces an attempt without interaction: not counted for most types, counted as an incomplete attempt for passkeys.

Never finish a subflow. The classifier derives each attempt's outcome from its steps, and the outcome-bearing step is `postResponse` for almost every subflow. Track that step always. For passkeys, also track the `ceremony` step: it powers all passkey analytics and nothing in it is recoverable from the server response alone.

Instrumentation is additive by default. It observes your app's existing lifecycle and adds no cancellation, timers or navigation rules of its own.

See [Subflows](/corbado-observe/tracking/subflows) for the helper reference.

## 5. Identity and tags

Call `setUser({ userId, identifier })` as soon as identity is known and while the flow is open: right after the step that resolved it, and at the latest before `flowFinished()`. The SDK records a `flow_enriched` event on the open flow. A skipped enrollment carries identity too, because the user is already known when the flow starts.

Tags ride flows. Configuration known at entry (product, variant, device class) goes on `flowStarted()`. Values only known on success go on `flowFinished()`. Do not re-fire the opener from reactive configuration such as store hydration or feature flags just to refresh its tags.

## 6. Journey catalog

The event series a correct integration produces for common journeys. Options are abbreviated and `spec` stands for `explicitSpecType`.

### 6.1 Identifier-first login, back, then sign-up

| Event Name | Data | Description |
| --- | --- | --- |
| `flow_started` | `flowName: login`, `touchpoint: account` | On page render. |
| `auth_method_decision_started` | `pre-identifier`, options `identifier-email`, `switch-to-signup`, `social-google` | The first checkpoint. |
| `subflow_started` | `provide-identifier`, spec `email` | Identifier field rendered. |
| `subflow_step_started` | `provide-identifier`, `pi-post-response` | Identifier submitted. |
| `subflow_step_finished` | `provide-identifier`, `pi-post-response` | Identifier accepted. Resolves `pre-identifier`. |
| `auth_method_decision_started` | `post-identifier`, options `password-login-known-identifier`, `passkey-login-known-identifier`, `back` | The methods offered once the identifier is known. |
| `subflow_started` | `password-login`, spec `password-known-identifier` | Password field rendered. Never submitted, so it has no step and is not counted. |
| `auth_method_decision_finished` | `post-identifier`, `explicitDecisionValue: back` | A navigational choice, so it is finished explicitly. |
| `auth_method_decision_started` | `pre-identifier`, same options as before | The same checkpoint, genuinely re-presented. A second occurrence, not a repeat. |
| `auth_method_decision_finished` | `pre-identifier`, `explicitDecisionValue: switch-to-signup` | |
| `flow_started` | `flowName: signup` | Nested in the open login flow. |
| `auth_method_decision_started` | `signup-registration`, options `password-enrollment`, `back` | |
| `subflow_started` | `password-enrollment`, spec `password-set` | |
| `subflow_step_started` | `password-enrollment`, `post-response` | Form submitted. |
| `subflow_step_finished` | `password-enrollment`, `post-response` | Resolves `signup-registration`. |
| `flow_enriched` | `userId`, `identifier` | Through `setUser()`. |
| `flow_finished` | `flowName: signup` | |
| `flow_auto_finished` | `flowName: login`, `finishedByFlowName: signup` | The nested sign-up completes the parent login. |

Note what is absent: no decision `finished` for the method choices, no subflow finishes and no explicit abandon events. Had the user left mid-journey, the missing finishes would have classified it. The second `pre-identifier` decision is deliberately a second occurrence: the user genuinely revisited that checkpoint. The password attempt that rendered but was never submitted has no step and is not counted; the `back` finish resolves the `post-identifier` occurrence it was rendered in. The step names differ per helper (`pi-post-response` for the identifier, `post-response` elsewhere); the helpers emit them, you never type them.

### 6.2 Direct sign-up, then a skipped enrollment prompt

| Event Name | Data | Description |
| --- | --- | --- |
| `flow_started` | `flowName: signup`, `touchpoint: account` | On page render. |
| `auth_method_decision_started` | `signup-registration`, options `password-enrollment` | A checkpoint with a single option still gets a decision. |
| `subflow_started` | `password-enrollment`, spec `password-set` | |
| `subflow_step_started` | `password-enrollment`, `post-response` | Form submitted. |
| `subflow_step_finished` | `password-enrollment`, `post-response` | Resolves `signup-registration`. |
| `flow_enriched` | `userId`, `identifier` | Through `setUser()`. |
| `flow_finished` | `flowName: signup` | The sign-up established the session on its own. |
| `flow_started` | `flowName: enrollment`, `touchpoint: post-signup` | Chained, not nested: the sign-up had already finished. |
| `auth_method_decision_started` | `enrollment-passkey`, options `passkey-enrollment`, `skip` | |
| `auth_method_decision_finished` | `enrollment-passkey`, `explicitDecisionValue: skip` | Skipping is navigational, so it is finished explicitly. |
| `flow_finished` | `flowName: enrollment`, `explicitOutcome: skipped` | |

The sign-up establishes the session on its own. There is no `login` flow to auto-finish here; `flowAutoFinished()` belongs only to a parent that was actually started (6.1, 6.4). The enrollment starts after the sign-up finished, so it is a sibling.

### 6.3 Combined form resolved to login

| Event Name | Data | Description |
| --- | --- | --- |
| `flow_started` | `flowNames: [login, signup]`, `defaultFlowName: login`, `touchpoint: checkout` | The form serves both journeys, so the flow opens undecided. |
| `auth_method_decision_started` | `pre-identifier`, options `identifier-email`, `social-google` | |
| `subflow_started` | `provide-identifier`, spec `email` | |
| `subflow_step_started` | `provide-identifier`, `pi-post-response` | Identifier submitted. |
| `subflow_step_finished` | `provide-identifier`, `pi-post-response` | |
| `flow_enriched` | `userId` | Through `setUser()`. |
| `flow_decided` | `flowName: login` | The identifier exists, so the flow resolves to login. |
| `auth_method_decision_started` | `post-identifier`, options `passkey-login-known-identifier`, `other-methods` | |
| `subflow_started` | `passkey-login`, spec `passkey-known-identifier` | Button clicked. |
| `subflow_step_started` / `subflow_step_finished` | `passkey-login`, `get-options` | |
| `subflow_step_started` / `subflow_step_finished` | `passkey-login`, `ceremony` | |
| `subflow_step_started` / `subflow_step_finished` | `passkey-login`, `post-response` | Resolves `post-identifier`. |
| `flow_finished` | `flowName: login`, `userId`, `identifier` | |

### 6.4 Recovery nested in login with automatic login

| Event Name | Data | Description |
| --- | --- | --- |
| `flow_started` | `flowName: login`, `touchpoint: account` | On page render. |
| … | | The identifier and password screens, omitted here. |
| `auth_method_decision_finished` | `post-identifier`, `explicitDecisionValue: forgot-password` | A navigational choice out of the checkpoint. |
| `flow_started` | `flowName: recovery`, `touchpoint: login` | Nested in the open login flow. |
| `subflow_started` | `email-link`, spec `email-link-login` | Runs without a decision of its own: no option resolves to it. |
| `subflow_step_started` / `subflow_step_finished` | `email-link`, `send` | |
| `flow_enriched` | `crossEnvironmentTransactionID` | Through `setCrossEnvironmentTransactionId()`. Joins the two sessions if the link opens elsewhere. |
| `subflow_step_started` / `subflow_step_finished` | `email-link`, `post-response` | The link was opened in the same browser. |
| `auth_method_decision_started` | `password-reset`, options `password-enrollment` | |
| `subflow_started` | `password-enrollment`, spec `password-reset` | |
| `subflow_step_started` / `subflow_step_finished` | `password-enrollment`, `post-response` | Resolves `password-reset`. |
| `flow_enriched` | `userId` | Through `setUser()`. |
| `flow_finished` | `flowName: recovery` | |
| `flow_auto_finished` | `flowName: login`, `finishedByFlowName: recovery` | The automatic login completes the parent. |

Two things this series shows. The email-link subflow runs without a decision of its own: no decision option resolves to it, so a decision rendered around it would only ever close incomplete. And the link opening on another device is a different session; that page starts its own `recovery` flow and calls `setCrossEnvironmentTransactionId()` with the same ID, which is what joins the two sessions. It has no open `login` to auto-finish.

### 6.5 Bank login with a second factor, a trusted browser and a chained enrollment

A returning customer with a remembered client number. The identifier step is skipped, the password is required, the second factor is an approval in the bank's app, and the approval also registers the browser as trusted until midnight. After login the customer is offered a passkey.

| Event Name | Data | Description |
| --- | --- | --- |
| `flow_started` | `flowName: login`, `touchpoint: netbank-logon`, `tags: { known_user: true }` | On page render. |
| `flow_enriched` | `identifier` | Through `setUser()`, the remembered client number. |
| `auth_method_decision_started` | `post-identifier`, options `forget-client`, `password-login-known-identifier`, `switch-to-passkey`, `back` | The identifier step is skipped, so the journey starts at this checkpoint. |
| `subflow_started` | `password-login`, spec `password-known-identifier` | Password field rendered. |
| `subflow_step_started` | `password-login`, `post-response` | Password submitted. |
| `subflow_step_finished` | `password-login`, `post-response` | Resolves `post-identifier`. |
| `subflow_started` | `trusted-device-check`, spec `token` | System work: the host checked the cookie. Belongs to no decision. |
| `subflow_step_started` | `trusted-device-check`, `post-response` | |
| `subflow_step_finished` | `trusted-device-check`, `post-response`, `result: not-trusted` | A negative check is a finished step with a result, never an error. |
| `auth_method_decision_started` | `2fa`, options `passkey-login-known-identifier`, `switch-to-app`, `back` | The second-factor checkpoint. |
| `auth_method_decision_finished` | `2fa`, `explicitDecisionValue: switch-to-app` | Navigational, so finished explicitly. |
| `auth_method_decision_started` | `2fa`, options `app-confirmation`, `try-another-way` | The same checkpoint, presented again on the app screen. |
| `subflow_started` | `app-confirmation` | The request is shown. |
| `subflow_step_started` | `app-confirmation`, `ceremony` | |
| `subflow_step_finished` | `app-confirmation`, `ceremony` | The poll saw the approval. |
| `subflow_step_started` | `app-confirmation`, `post-response` | The continuation creates the session. |
| `subflow_step_finished` | `app-confirmation`, `post-response` | Resolves the second `2fa` occurrence. |
| `subflow_started` | `trusted-device-enrollment`, spec `token` | System work again, no decision. |
| `subflow_step_started` | `trusted-device-enrollment`, `post-response` | |
| `subflow_step_finished` | `trusted-device-enrollment`, `post-response`, `result: created` | The browser is trusted until midnight. |
| `flow_enriched` | `userId`, `identifier` | Through `setUser()`, now host-confirmed. |
| `flow_finished` | `flowName: login`, `tags: { method: app, browser_trusted: true }` | |
| `flow_started` | `flowName: enrollment`, `touchpoint: security-settings` | Chained after the login finished, so a sibling. |
| `flow_enriched` | `userId` | Through `setUser()`. Its own identity call. |
| `auth_method_decision_started` | `enrollment-user`, options `passkey-enrollment`, `skip`, `dont-ask-again` | |
| `subflow_started` | `passkey-enrollment`, spec `manual` | |
| `subflow_step_started` / `subflow_step_finished` | `passkey-enrollment`, `get-options` | |
| `subflow_step_started` / `subflow_step_finished` | `passkey-enrollment`, `ceremony`, `mediation: required` | |
| `subflow_step_started` / `subflow_step_finished` | `passkey-enrollment`, `post-response` | Resolves `enrollment-user`. |
| `flow_finished` | `flowName: enrollment` | A `dont-ask-again` would finish with `explicitOutcome: skipped`, exactly like a skip. |

Three things this series shows. The two trusted-device subflows are system work: they belong to no decision and resolve no option, and the negative check is a finished step with a result, not an error. The identifier is attached as soon as it is known, here at bootstrap because the browser remembered the client, otherwise right after the customer submits it. The confirmed user id follows with the login result. The `2fa` checkpoint is presented twice because the customer navigated from the method choice to the app screen, and the second presentation is the one the app confirmation resolves. The enrollment is chained after the login finished, so it is a sibling with its own identity call, and a "don't ask again" would finish it with `explicitOutcome: skipped` exactly like a skip.

### 6.6 Combined login form, password used

One screen offers everything: identifier and password together, a passkey button and a social button. Conditional UI is armed on the same screen, so autofill is offered too. The customer uses the form.

```text Screen
┌────────────────────────────────────────────┐
│  Sign in                                   │
├────────────────────────────────────────────┤
│           ┌───────────────────────────┐    │
│  Email    │                           │    │  <-- 1) types the email
│           └───────────────────────────┘    │
│           ┌───────────────────────────┐    │
│  Password │                           │    │  <-- 2) types the password
│           └───────────────────────────┘    │
│       ┌────────────────────────────┐       │
│       │          Sign in           │       │  <-- 3) presses Sign in
│       └────────────────────────────┘       │
├───────────────────  or  ───────────────────┤
│       ┌────────────────────────────┐       │
│       │   Sign in with a passkey   │       │
│       └────────────────────────────┘       │
│       ┌────────────────────────────┐       │
│       │    Continue with Google    │       │
│       └────────────────────────────┘       │
└────────────────────────────────────────────┘
```

| Event Name | Data | Description |
| --- | --- | --- |
| `flow_started` | `flowName: login`, `touchpoint: account` | On page render. |
| `auth_method_decision_started` | `pre-identifier`, options `password-login-with-identifier`, `passkey-login-cui`, `passkey-login-no-identifier`, `social-google` | On page render, once the screen knows what it offers. |
| `subflow_started` | `password-login`, spec `password-with-identifier` | On page render, when the form appears. The helper is bound to the password input. |
| `subflow_step_started` | `password-login`, `post-response` | After **3)**, when the request leaves the browser. |
| `subflow_step_finished` | `password-login`, `post-response` | When the server accepts the credentials. Resolves `pre-identifier`. |
| `flow_enriched` | `userId`, `identifier` | As soon as identity is known, through `setUser()`. |
| `flow_finished` | `flowName: login` | On success. |

Steps 1 and 2 produce no tracking events. The helper is bound to the password input, so typing is recorded as low-level interaction signals that ride along with the next batch, not as events you emit. The first event of the attempt is the submit.

There is one subflow and it opened on render, before the customer touched anything. That is correct for an input-bound method: the field is the attempt surface. It becomes a real attempt when the step arrives, and the finished step resolves the checkpoint.

### 6.7 Combined login form, passkey used from autofill

The same screen. The customer never types: the browser offers a passkey in the email field and they pick it.

```text Screen
┌────────────────────────────────────────────┐
│  Sign in                                   │
├────────────────────────────────────────────┤
│           ┌───────────────────────────┐    │
│  Email    │                           │    │  <-- 1) focuses the field, sees a passkey
│           └───────────────────────────┘    │
│           ┌───────────────────────────┐    │
│  Password │                           │    │
│           └───────────────────────────┘    │
│       ┌────────────────────────────┐       │
│       │          Sign in           │       │
│       └────────────────────────────┘       │
├───────────────────  or  ───────────────────┤
│       ┌────────────────────────────┐       │
│       │   Sign in with a passkey   │       │
│       └────────────────────────────┘       │
│       ┌────────────────────────────┐       │
│       │    Continue with Google    │       │
│       └────────────────────────────┘       │
└────────────────────────────────────────────┘
```

| Event Name | Data | Description |
| --- | --- | --- |
| `flow_started` | `flowName: login`, `touchpoint: account` | On page render. |
| `auth_method_decision_started` | `pre-identifier`, options `password-login-with-identifier`, `passkey-login-cui`, `passkey-login-no-identifier`, `social-google` | On page render, once the screen knows what it offers. |
| `subflow_started` | `password-login`, spec `password-with-identifier` | On page render, when the form appears. |
| `subflow_step_started` / `subflow_step_finished` | `password-login`, `cui-get-options` | Armed on render, before any interaction. Mark it `ignoreAsInteraction` so an unused request does not count as engagement. |
| `subflow_step_started` | `password-login`, `cui-ceremony` | After **1)**, when the browser shows the suggestion. |
| `subflow_step_finished` | `password-login`, `cui-ceremony` | When the customer picks the passkey from the suggestion. |
| `subflow_step_started` / `subflow_step_finished` | `password-login`, `cui-post-response` | Resolves `passkey-login-cui`. |
| `flow_enriched` | `userId` | Through `setUser()`. |
| `flow_finished` | `flowName: login` | On success. |

No `passkey-login` subflow appears. Conditional UI on this screen belongs to the form helper, which carries it as `cui` steps, so the attempt stays one `password-login` subflow and resolves `passkey-login-cui` instead of `passkey-login-no-identifier`.

`cui-get-options` fires on render, before any interaction. Mark it `ignoreAsInteraction` so an armed request that nobody used does not count as the customer engaging with the form.

### 6.8 Combined login form, passkey button used

The same screen. The customer ignores the form and presses the passkey button.

```text Screen
┌────────────────────────────────────────────┐
│  Sign in                                   │
├────────────────────────────────────────────┤
│           ┌───────────────────────────┐    │
│  Email    │                           │    │
│           └───────────────────────────┘    │
│           ┌───────────────────────────┐    │
│  Password │                           │    │
│           └───────────────────────────┘    │
│       ┌────────────────────────────┐       │
│       │          Sign in           │       │
│       └────────────────────────────┘       │
├───────────────────  or  ───────────────────┤
│       ┌────────────────────────────┐       │
│       │   Sign in with a passkey   │       │  <-- 1) presses the button
│       └────────────────────────────┘       │
│       ┌────────────────────────────┐       │
│       │    Continue with Google    │       │
│       └────────────────────────────┘       │
└────────────────────────────────────────────┘
```

| Event Name | Data | Description |
| --- | --- | --- |
| `flow_started` | `flowName: login`, `touchpoint: account` | On page render. |
| `auth_method_decision_started` | `pre-identifier`, options `password-login-with-identifier`, `passkey-login-cui`, `passkey-login-no-identifier`, `social-google` | On page render, once the screen knows what it offers. |
| `subflow_started` | `password-login`, spec `password-with-identifier` | On page render, when the form appears. |
| `subflow_started` | `passkey-login`, spec `passkey-no-identifier` | After **1)**, on the press. Not when the button becomes visible. |
| `subflow_step_started` / `subflow_step_finished` | `passkey-login`, `get-options` | |
| `subflow_step_started` / `subflow_step_finished` | `passkey-login`, `ceremony` | While the browser dialog is open and the customer picks a passkey. |
| `subflow_step_started` / `subflow_step_finished` | `passkey-login`, `post-response` | Resolves `pre-identifier`. |
| `flow_enriched` | `userId` | Through `setUser()`. |
| `flow_finished` | `flowName: login` | On success. |

Both subflows open, and only one is an attempt the customer made. The password helper is input-bound and opens on render; it produces no step, carries no interaction and resolves nothing, which is how an offered-but-untouched method is supposed to look.

The passkey helper is action-bound and opens on the press, not when the button becomes visible. Creating it on render would book an attempt nobody made, and because the checkpoint is resolved by the subflow that completes under it, an untouched offer would capture `pre-identifier` on every visit.

### 6.9 Combined login form, social login used

The same screen. The customer presses the social button and returns from the provider.

```text Screen
┌────────────────────────────────────────────┐
│  Sign in                                   │
├────────────────────────────────────────────┤
│           ┌───────────────────────────┐    │
│  Email    │                           │    │
│           └───────────────────────────┘    │
│           ┌───────────────────────────┐    │
│  Password │                           │    │
│           └───────────────────────────┘    │
│       ┌────────────────────────────┐       │
│       │          Sign in           │       │
│       └────────────────────────────┘       │
├───────────────────  or  ───────────────────┤
│       ┌────────────────────────────┐       │
│       │   Sign in with a passkey   │       │
│       └────────────────────────────┘       │
│       ┌────────────────────────────┐       │
│       │    Continue with Google    │       │  <-- 1) presses the button
│       └────────────────────────────┘       │
└────────────────────────────────────────────┘
```

| Event Name | Data | Description |
| --- | --- | --- |
| `flow_started` | `flowName: login`, `touchpoint: account` | On page render. |
| `auth_method_decision_started` | `pre-identifier`, options `password-login-with-identifier`, `passkey-login-cui`, `passkey-login-no-identifier`, `social-google` | On page render, once the screen knows what it offers. |
| `subflow_started` | `password-login`, spec `password-with-identifier` | On page render, when the form appears. |
| `subflow_started` | `social-login`, spec `pre-identifier` | After **1)**, on the press. |
| `subflow_step_started` / `subflow_step_finished` | `social-login`, `get-redirect-url` | Only when your backend builds the authorization URL. Omit it when the provider SDK builds it in the browser. |
| `subflow_step_started` / `subflow_step_finished` | `social-login`, `exchange-code` | The outcome-bearing step. Resolves `social-google`. |
| `flow_enriched` | `userId`, `identifier` | Through `setUser()`. |
| `flow_finished` | `flowName: login` | On success. |

`get-redirect-url` only belongs here when your backend builds the authorization URL. Providers whose SDK builds it in the browser never produce that step, and inventing one would report work that did not happen. `exchange-code` is the outcome-bearing step either way.

The subflow opens on the press. A social button that merely rendered is an offer, already carried by `social-google` in the option set, and opening a subflow for it would resolve the checkpoint to the provider on every visit.

## 7. What the classifier merges, repairs or drops

| Signal | Classifier behavior |
| --- | --- |
| Repeated `flow_started` for the innermost open flow | Merged. Latest touchpoint wins, tags accumulate. |
| Repeated `flow_started` for an outer flow while a nested flow is open | Merged into the outer flow. Every flow nested under it closes as incomplete. |
| `flow_finished` or `flow_decided` without an open flow | Fatal. The whole session scope fails classification. |
| `flow_auto_finished` without an open flow of that name | Reconstructed as an already-closed flow of that name. Meant for the sibling the classifier closed on `flow_decided`; never send it for a flow that was not started. |
| Consecutive `subflow_started` of the same type | Folded into one attempt. A different, known spec type starts a new attempt. |
| Decision `started` re-emitted with the same or changed options | New occurrence. The open one closes as incomplete. |
| Decision `started` re-emitted with the same `explicitTimestamp` | Replaces the open occurrence in place. |
| Decision `finished` without an open decision of that name | A zero-duration completed occurrence. |
| Decision `finished` whose value is not one of the declared options | Completed with that value, but invisible to option-level reports. Keep values inside the declared set. |
| Subflow attempt with no step and no observed input interaction | Not counted. A passkey attempt is the exception: counted as incomplete, resolving no decision. |
| Passkey, password-enrollment, provide-data, email-link, social or TOTP attempt without a spec type on any event | Dropped. Password login, identifier entry, OTP and app confirmation default instead. |
| Events arriving out of order within a screen | Repaired. The backend orders by timestamp and emission sequence and tolerates known races. |

## 8. Next steps

<CardGroup cols={2}>
  <Card title="Flows" icon="diagram-project" href="/corbado-observe/tracking/flows">
    Flow events, nesting and outcomes.
  </Card>
  <Card title="Decisions" icon="code-branch" href="/corbado-observe/tracking/decisions">
    Presentation rules and option strings.
  </Card>
  <Card title="Subflows" icon="list-check" href="/corbado-observe/tracking/subflows">
    Operation helpers, steps and spec types.
  </Card>
  <Card title="Use with AI agents" icon="robot" href="/corbado-observe/get-started/use-with-ai-agents">
    Let an agent apply this method to your codebase.
  </Card>
</CardGroup>
