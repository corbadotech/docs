---
title: "Command: Login"
description: "Use the login command to log into the Corbado CLI."
---

The login command asks for your `projectID` and `cliSecret` and saves them to a credentials file locally. So you can login once and do not have to provide the `projectID` and `cliSecret` every time you run a command.

`corbado login`