---
title: "Setup"
description: "Learn how to add the Corbado web-js package to your Vue.js application."
---

import Installation from "/snippets/corbado-complete/web-js-prerequisites.mdx";
import CorbadoProviderReference from "/snippets/corbado-complete/corbadoprovider-api-reference.mdx";

## Installation

<Installation />

## Set up Corbado

As a prerequisite to all future actions, Corbado has to be loaded.
We do this by using the [Corbado.load](/corbado-complete/frontend-integration/vue-js) function.
We also maintain a global state, indicating whether Corbado is successfully loaded.

```html App.vue
<script setup lang="ts">
import Corbado from "@corbado/web-js"
import { ref, provide, onMounted } from 'vue'

// Create a global state
const isCorbadoLoaded = ref(false)

onMounted(async () => {
    await Corbado.load({
        projectId: "pro-XXX"
    })
    isCorbadoLoaded.value = true // Set the global state to true after Corbado is loaded
})

// Provide the state globally
provide('isCorbadoLoaded', isCorbadoLoaded)
</script>

<template>
    <!-- Your app content -->
</template>
```

<CorbadoProviderReference isWebJS={true} />

## Next

1. Add [authentication UI components](/corbado-complete/frontend-integration/vue-js/corbadoauth) to your application.
2. Let your users manage their passkeys with the [PasskeyList](/corbado-complete/frontend-integration/vue-js/passkeylist).
3. Learn how you can [access the users authentication state](/corbado-complete/frontend-integration/vue-js/authentication-state).