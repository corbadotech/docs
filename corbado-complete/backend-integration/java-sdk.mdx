---
title: "Java"
description: "Use the official Corbado Java SDK to simplify the passkey and session management integration in your Java applications."
---

import SessionTokenIntro from "/snippets/corbado-complete/session-token-intro.mdx"
import SessionTokenValidation from "/snippets/corbado-complete/session-token-validation.mdx"
import SessionTokenWhyImportant from "/snippets/corbado-complete/session-token-why-important.mdx"

<SessionTokenIntro />

<SessionTokenValidation />

## Implementation

Below is an example of how to use the `validateToken()` function. As previously mentioned, by default, the **session-token** is stored in a cookie and sent to your backend. You will need to retrieve this cookie based on the HTTP framework you are using.

<Info>
  You can find instructions for installing and initializing the Corbado Java SDK in the official [README](https://github.com/corbado/corbado-java).
</Info>


```java
import com.corbado.Corbado;
import com.corbado.CorbadoConfig;

// Initialize SDK with your project ID, API secret, and optionally backend API URL
Config config = Config.builder().apiSecret(apiSecret).projectId(projectId).build();
CorbadoSDK sdk = new CorbadoSDK(config);

// Retrieve session-token (e.g., from a cookie or header)
String sessionToken = "...";

// Validate session-token
User user = sdk.sessions().validateToken(sessionToken);
// user.getUserId() etc. are now available
```
<SessionTokenWhyImportant />

For more details, see the [Corbado Java SDK documentation](https://github.com/corbado/corbado-java).


