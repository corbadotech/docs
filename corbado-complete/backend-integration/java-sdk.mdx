---
title: "Java"
description: "Use the official Corbado Java SDK to simplify the passkey and session management integration in your Java applications."
---

After integrating Corbado in your frontend, your users authenticate and receive a session token. Pass this token (from a cookie or header) to your backend and validate it as shown below. [Learn more about session token handling.](/corbado-complete/sessions/session-token-handling)

The Corbado Java SDK can be found on [GitHub](https://github.com/corbado/corbado-java). The [README](https://github.com/corbado/corbado-java) there contains detailed information on how to install and use the Corbado Java SDK.

To help you securely validate session tokens in your Java backend, the following sections explain how the `validateToken()` function works and how to use it in practice.

---

## Token Validation with `validateToken()`

```java
import com.corbado.Corbado;
import com.corbado.CorbadoConfig;

// Example initialization of Config class with builder (preferred):
Config config = null;
if (StringUtils.isEmpty(backendApi)) {
    config = Config.builder().apiSecret(apiSecret).projectId(projectId).build();
} else {
    config = Config.builder().apiSecret(apiSecret).projectId(projectId).backendApi(backendApi).build();
}
CorbadoSDK sdk = new CorbadoSDK(config);

// Alternative initialization with 'new':
final Config configAlt = new Config(projectId, apiSecret);
CorbadoSDK sdkAlt = new CorbadoSDK(configAlt);

// Get sessionToken from cookie or Authorization header
// See session token handling for details: https://docs.corbado.com/corbado-complete/sessions/session-token-handling
String sessionToken = "...";
User user = sdk.sessions().validateToken(sessionToken);
// user.getUserId() etc. are now available
```

### What does `validateToken()` do?
- Verifies the JWT signature using Corbado's public keys (JWKS).
- Checks the token's expiration and validity.
- Returns the decoded claims if the token is valid, or throws an error otherwise.

### Why is this important?
- Ensures only valid, untampered tokens are accepted.
- Protects your backend endpoints from unauthorized access.
- Handles key rotation and validation logic for you.

For more details, see the [Corbado Java SDK documentation](https://github.com/corbado/corbado-java).
