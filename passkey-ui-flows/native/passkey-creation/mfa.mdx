---
title: "Passkey Creation: After Login of MFA Account (post-login nudge)"
sidebarTitle: "After Login: MFA Account"
description: "Native flow for offering passkey creation after the user completes the account's MFA sign-in policy."
---

import { ToolingFrame } from "/snippets/authentication-flow/tooling-frame.mdx";
import N22AcceptanceCriteria from "/snippets/passkey-ui-flows/acceptance-criteria/n2-2.mdx";

## Passkey Creation For MFA User Accounts

After the account's complete MFA sign-in, the app can offer passkey creation for future sign-ins.

<Tabs>
<Tab title="iOS">
    <ToolingFrame 
        imageURL="/images/authentication-flow/native-app/mfa-append-ios-native.webp"
        caption="Native: iOS passkey append, MFA accounts"
        imageName="mfa-append-ios-native.webp"
    />
</Tab>

  <Tab title="Android">
    <ToolingFrame 
        imageURL="/images/authentication-flow/native-app/mfa-append-android-native.webp"
        caption="Native: Android passkey append, MFA accounts"
        imageName="mfa-append-android-native.webp"
    />
  </Tab>
</Tabs>

<Note>
    Blue steps in the diagram are product-specific and are not part of the passkey benchmark.
</Note>
<Steps>
    <Step title="Login when user has no passkeys and has activated MFA">
        - User enters their identifier (email/username) on the login page.
    </Step>
    <Step title="Fallback to traditional login method">
        - If no passkey exists for the account, the system falls back to the traditional login method (username and password).
    </Step>
    <Step title="Complete the configured MFA policy">
        - After the primary factor, the user completes every additional factor required by the account's current policy.
    </Step>
    <Step title="Passkey append process initiates">
        - Show a manual nudge that explains how a passkey improves the next sign-in.
        - A quality implementation can first open one creation ceremony automatically. If it is cancelled or fails, show the manual nudge without an error.
    </Step>
    <Step title="System passkey-creation sheet appears">
        - In the manual flow, the sheet appears after the user clicks Continue.
        - The platform asks the user to authorize creation with a supported device-unlock method.
    </Step>
    <Step title="Passkey creation successful">
        - User successfully creates a passkey and it is now linked with the user account for the respective native app.
    </Step>
    <Step title="Continue to the app">
        - Skipping the optional nudge continues without a passkey.
        - After the configured limit of cancelled or failed attempts, the app continues without another prompt or error.
    </Step>

</Steps>

<N22AcceptanceCriteria />
