---
title: "Passkey Creation: After Login of MFA Account (post-login nudge)"
sidebarTitle: "After Login: MFA Account"
description: "Web flow for offering passkey creation after the user completes the account's MFA sign-in policy."
---

import { ToolingFrame } from "/snippets/authentication-flow/tooling-frame.mdx";
import W23AcceptanceCriteria from "/snippets/passkey-ui-flows/acceptance-criteria/w2-3.mdx";

## Passkey Creation for MFA User Accounts

MFA users logging in via desktop and mobile browsers follow a standard login flow where they are offered the option to append a passkey.

<ToolingFrame
    imageURL="/images/authentication-flow/web-app/desktop/mfa-append-macos.webp"
    caption="Representative web passkey creation flow for an MFA account shown on macOS"
    imageName="mfa-append-macos.webp"
/>
<Info>
    The blue background indicates product-specific steps, such as a verification
    code. Passkey creation occurs within the same application flow.
</Info>

<Steps>
    <Step title="Login when user has no passkeys and has activated MFA">
        - User enters their identifier (email/username) on the login page.
    </Step>
    <Step title="Fallback to traditional login method">
        - If no passkey exists for the account, the system falls back to the traditional login method (username and password).
    </Step>
    <Step title="Complete configured additional-factor challenge">
        - After the first sign-in step, the user completes every additional factor required by the account's active policy. The benchmark does not prescribe email, SMS, an authenticator app, push, a security key or another specific factor.
    </Step>
    <Step title="Passkey append process initiates">
        - Once MFA succeeds, the application shows a manual creation nudge that explains the future sign-in benefit.
        - A quality implementation can first open one creation ceremony automatically. If the user cancels it, the manual nudge appears without an error and waits for the user to click Continue before trying again.
    </Step>
    <Step title="System passkey-creation sheet appears">
        - User authorizes creation with a platform-supported device-unlock method.
    </Step>
    <Step title="Passkey creation successful">
        - User successfully creates a passkey and it is now linked with the user account for the respective web-app.
    </Step>
    <Step title="Redirection to app dashboard">
        - User is redirected to the app dashboard.
    </Step>

</Steps>

<W23AcceptanceCriteria />
