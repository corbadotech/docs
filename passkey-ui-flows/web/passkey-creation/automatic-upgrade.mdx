---
title: "Passkey Creation: Automatic Password to Passkey Upgrade (conditional create)"
sidebarTitle: "Automatic Upgrade"
description: "Conditional passkey creation after browser sign-in with a password supplied by the user's credential manager."
---

import { ToolingFrame } from "/snippets/authentication-flow/tooling-frame.mdx";
import W21AcceptanceCriteria from "/snippets/passkey-ui-flows/acceptance-criteria/w2-1.mdx";

<Info>
    **Support (August 2026):** Chrome 136+ on desktop and Chrome 142+ on Android with Google Password Manager, Safari 18+ / macOS 15+ and all browsers on iOS 18+ with Apple Passwords. Firefox does not support it and third-party password manager coverage is still uneven. It requires a recent password-manager fill. The active credential provider decides whether its internal conditions are met, while the relying party supplies known credential IDs to prevent avoidable duplicates.
</Info>

## Automatic Passkey Creation after Login (Conditional Create)

Users logging in via a browser follow a standard login flow enriched by automatic passkey creation via Conditional Create after successfully using a password manager to autofill their password.

**[Passkey Intelligence](/corbado-connect/features/passkey-intelligence)** is one implementation of the eligibility logic. Automatic creation is attempted only when the required conditions are met.

<Tabs>
  <Tab title="macOS">
    <ToolingFrame 
        imageURL="/images/authentication-flow/web-app/desktop/automatic-passkey-upgrade-macos.webp"
        caption="macOS automatic passkey upgrade flow"
        imageName="automatic-passkey-upgrade-macos.webp"
    />
  </Tab>

<Tab title="Windows">
    <ToolingFrame
        imageURL="/images/authentication-flow/web-app/desktop/automatic-passkey-upgrade-windows.webp"
        caption="Windows automatic passkey upgrade flow"
        imageName="automatic-passkey-upgrade-windows.webp"
    />
</Tab>

<Tab title="iOS">
    <ToolingFrame 
        imageURL="/images/authentication-flow/web-app/mobile/automatic-passkey-upgrade-ios.webp"
        caption="iOS automatic passkey upgrade flow"
        imageName="automatic-passkey-upgrade-ios.webp"
    />
</Tab>
</Tabs>
<Info>
    The blue background indicates product-specific steps, such as a verification
    code. Passkey creation occurs within the same application flow.
</Info>

<Steps>
  <Step title="Login with password autofill">
    - User logs in with their password, which is then autofilled by e.g. Apple Passwords / Google Password Manager. 
  </Step>
  <Step title="Credential manager authorizes password autofill where required">
    - The active credential manager may require a supported device-unlock method before filling the saved password. The exact modality and whether a prompt appears are platform-dependent.
  </Step>
  <Step title="Complete configured sign-in verification">
    - The user completes any additional verification required by the relying party's sign-in policy. This step is omitted when no additional factor is required.
  </Step>
  <Step title="Automatic passkey creation">
    - After the configured sign-in has completed, the application promptly requests Conditional Create (`navigator.credentials.create({ mediation: 'conditional' })`). The credential provider decides whether the request is eligible and can silently decline it.
    - If the provider returns a credential and the server verifies and stores it, creation succeeds and provider or operating-system feedback may appear.
    - If the provider silently declines or returns no credential, sign-in continues without a success state; the normal explicit enrollment route remains available.
  </Step>
  <Step title="Redirection to app dashboard">
    - User is redirected to the app dashboard.
  </Step>

</Steps>

<W21AcceptanceCriteria />
