---
title: "Passkey Creation: Automatic Password to Passkey Upgrade (conditional create)"
sidebarTitle: "Automatic Upgrade"
description: "This flow explains the automatic passkey creation feature that browsers provide when the user used autofill to log in with a password from a password manager."
---

import { ToolingFrame } from "/snippets/authentication-flow/tooling-frame.mdx";

<Info>
    **Support (August 2026):** Chrome 136+ on desktop and Chrome 142+ on Android with Google Password Manager, Safari 18+ / macOS 15+ and all browsers on iOS 18+ with Apple Passwords. Firefox does not support it, and third-party password manager coverage is still uneven. It only triggers when the password was filled by the user's credential manager, and only when no passkey exists yet for the account.
</Info>

## Automatic Passkey Creation after Login (Conditional Create)

Users logging in via a browser follow a standard login flow enriched by automatic passkey creation via Conditional Create after successfully using a password manager to autofill their password.

**[Passkey Intelligence](/corbado-connect/features/passkey-intelligence)** works in the background to enable this feature, ensuring automatic passkey creation only happens when conditions are optimal for success.

<Tabs>
  <Tab title="macOS">
    <ToolingFrame 
        imageURL="/images/authentication-flow/web-app/desktop/automatic-passkey-upgrade-macos.webp"
        caption="macOS automatic passkey upgrade flow"
        imageName="automatic-passkey-upgrade-macos.webp"
    />
  </Tab>

<Tab title="Windows">
    <ToolingFrame
        imageURL="/images/authentication-flow/web-app/desktop/automatic-passkey-upgrade-windows.webp"
        caption="Windows automatic passkey upgrade flow"
        imageName="automatic-passkey-upgrade-windows.webp"
    />
</Tab>

<Tab title="iOS">
    <ToolingFrame 
        imageURL="/images/authentication-flow/web-app/mobile/automatic-passkey-upgrade-ios.webp"
        caption="iOS automatic passkey upgrade flow"
        imageName="automatic-passkey-upgrade-ios.webp"
    />
</Tab>
</Tabs>
<Info>
    NOTE: The blue background indicates product-native steps (e.g., verification
    code). Passkey creation is seamlessly embedded in your
    product journey.
</Info>

<Steps>
  <Step title="Login with password autofill">
    - User logs in with their password, which is then autofilled by e.g. Apple Passwords / Google Password Manager. 
  </Step>
  <Step title="Biometric confirmation for password auto-fill">
    - User confirms their identity with a biometric method (like Face ID or Touch ID)
  </Step>
  <Step title="Provide second factor">
    - User is asked to authenticate with a second factor (e.g. SMS OTP).
  </Step>
  <Step title="Automatic passkey creation">
    - [Passkey Intelligence](/corbado-connect/features/passkey-intelligence) determines conditions are optimal for automatic passkey creation.
    - The passkey is created in the background via conditional create (`navigator.credentials.create({ mediation: 'conditional' })`), given that the user has successfully authenticated with the second factor and no passkey exists yet.
    - Passkey is successfully created and the user is notified by the browser or operating system.
  </Step>
  <Step title="Redirection to app dashboard">
    - User is redirected to the app dashboard.
  </Step>

</Steps>
