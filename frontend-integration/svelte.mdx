---
title: "Svelte"
description: "Learn how to add the Corbado web-js package to your Svelte or SvelteKit application."
---

import Blogpost from "/snippets/blogpost.mdx";
import Prerequisites from "/snippets/web-js-prerequisites.mdx";

## 1. Prerequisites

<Prerequisites />

## 2. Add web-js to Svelte code

```html +page.svelte
<script lang="ts">
    import Corbado from "@corbado/web-js";
    import { onMount } from "svelte";

    onMount(async () => {
        await Corbado.load({
            projectId: "your-project-id"
        });

        const authElement = document.getElementById("corbado-auth"); //Element where you want to render CorbadoAuth UI

        Corbado.mountAuthUI(authElement!, {
            onLoggedIn: () => {
                //post login actions can be performed here.
            }
        });
    });
</script>

<div id="corbado-auth"></div>
```

If you need access to user data on the server, also take a look at our [Corbado Node.js SDK](/backend-integration/node-js-sdk).

## 3. Read the blog post

To find a step-by-step tutorial on how to integrate passkeys into Svelte, please check out our blog post:

<Card href="https://www.corbado.com/blog/svelte-passkeys">
    <Blogpost title="Building a Svelte Passkey Login" />
</Card>
