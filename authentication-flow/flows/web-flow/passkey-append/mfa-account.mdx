---
title: "Passkey Append: MFA Account"
description: "This flow explains how users append a passkey to a MFA account."
---

## Passkey Creation for MFA User Accounts

MFA users logging in via desktop and mobile browsers, follow a standard login flow where they are offered to append a passkey.

<Tabs>
  <Tab title="macOS">
    <Frame caption="macOS passkey append, MFA accounts">
      <img src="/images/authentication-flow/web-app/desktop/mfa-append-macos.png" alt="Web app desktop passkey append with MFA accounts on macOS" />
    </Frame>
  </Tab>

<Tab title="Windows">
    <Frame caption="Windows passkey append, MFA accounts">
        <img
            src="/images/authentication-flow/web-app/desktop/mfa-append-windows.png"
            alt="Web app desktop passkey append with MFA accounts on Windows"
        />
    </Frame>
</Tab>

<Tab title="iOS">
    <Frame caption="iOS passkey append, MFA accounts">
        <img
            src="/images/authentication-flow/web-app/mobile/mfa-append-ios.png"
            alt="Web app mobile passkey append with MFA accounts on iOS"
        />
    </Frame>
</Tab>

  <Tab title="Android">
    <Frame caption="Android passkey append, MFA accounts">
      <img src="/images/authentication-flow/web-app/mobile/mfa-append-android.png" alt="Web app mobile passkey append with MFA accounts on Android" />
    </Frame>
  </Tab>
</Tabs>
<Info>
    NOTE: The blue background indicates product-native steps (e.g., verification
    code). Passkey creation is seamlessly embedded in your
    product journey.
</Info>

<Steps>
    <Step title="Login when user has no passkeys and activated MFA">
        - User enters their identifier (email/username) on the login page.
    </Step>
    <Step title="Fallback to traditional login method">
        - System detects no passkey created for the respective account, system fallbacks to traditional login method (username + password).
    </Step>
    <Step title="System prompts for user verification">
        - After successful password authentication, the user is prompted to verify their account via a code sent through email or SMS.
    </Step>
    <Step title="Passkey append process initiates">
        - Once the system verifies user identity, the user is offered an option to create a passkey for future logins.
    </Step>
    <Step title="OS-native biometric modal appears">
        - User chooses & continue to set up a passkey using fingerprint, face, or PIN for secure sign-in.
    </Step>
    <Step title="Passkey creation successful">
        - User successfully creates passkey and is now linked with the user account for the respective web-app.
    </Step>
    <Step title="Redirection to app dashboard">
        - User is redirected to the app dashboard.
    </Step>

</Steps>
