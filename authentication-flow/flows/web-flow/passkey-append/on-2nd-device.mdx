---
title: "Passkey Append: On 2nd Device"
description: "This flow explains how users can append a passkey to their account after authenticating on a new (second) device with a password and MFA. This enables seamless, passwordless sign-in on future logins, strengthening security and improving the user experience."
---

## Passkey Creation on Second Device

Users logs in desktop or mobile web app from a new device, follows a standard hybrid login flow and experience passkey creation on the current device.

<Tabs>
<Tab title="macOS">
    <Frame caption="macOS passkey append on new device">
      <img src="/images/authentication-flow/web-app/desktop/append-on-second-device-macos.png" alt="Web app desktop passkey append on new device, on macOS" />
    </Frame>
  </Tab>

<Tab title="Windows">
    <Frame caption="Windows passkey append on new device">
        <img
            src="/images/authentication-flow/web-app/desktop/append-on-second-device-windows.png"
            alt="Web app desktop passkey append on new device, on Windows"
        />
    </Frame>
</Tab>
<Tab title="iOS">
    <Frame caption="iOS passkey append on new device">
        <img
            src="/images/authentication-flow/web-app/mobile/append-on-second-device-ios.png"
            alt="Web app mobile passkey append on new device, on iOS"
        />
    </Frame>
</Tab>

  <Tab title="Android">
    <Frame caption="Android passkey append on new device">
      <img src="/images/authentication-flow/web-app/mobile/append-on-second-device-android.png" alt="Web app mobile passkey append on new device, on Android" />
    </Frame>
  </Tab>
</Tabs>

<Info>
    NOTE: The blue background indicates product-native steps (e.g., verification
    code, setup PIN, enabling biometrics). Passkey creation is seamlessly
    embedded in your product journey.
</Info>
<Steps>
     <Step title="Login on current device using another device">
        - User enters their identifier (email/username) on the login page.
        - OS detects passkey linked to this account on another device.
        - User scans the QR code/ bluetooth connect and authenticates on the device having passkeys.
        - After successful authentication, user is asked to add a passkey on the current device as well.
        - User continues to create passkey. After successful passkey creation on the current device, the user is asked to setup a PIN and for biometrics enabling.
    </Step>

</Steps>
