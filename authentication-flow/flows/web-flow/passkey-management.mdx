---
title: "Passkey Management"
description: "This flow describes how users can manage their passkeys within the profile section of their account. Passkey management allows users to register, delete, and troubleshoot passkeys directly from their account’s security settings. "
---

## Create, Manage or Delete Passkeys via Passkey List Component

Passkey management lets user view, update, and remove the passkeys saved on your devices. It ensures user stay in control of which accounts are linked to which devices, and gives them the option to revoke access if a device is lost or no longer in use.

### General Passkey Management

<Tabs>
  <Tab title="macOS">
    <Frame caption="macOS passkey management flow">
      <img src="/images/authentication-flow/web-app/desktop/general-passkey-management-macos.png" alt="Web app desktop passkey management flow on macOS" />
    </Frame>
  </Tab>

<Tab title="Windows">
    <Frame caption="Windows passkey management flow">
        <img
            src="/images/authentication-flow/web-app/desktop/general-passkey-management-windows.png"
            alt="Web app desktop passkey management flow on Windows"
        />
    </Frame>
</Tab>
<Tab title="iOS">
    <Frame caption="iOS passkey management flow">
        <img
            src="/images/authentication-flow/web-app/mobile/general-passkey-management-ios.png"
            alt="Web app mobile passkey management flow on iOS"
        />
    </Frame>
</Tab>

  <Tab title="Android">
    <Frame caption="Android passkey management flow">
      <img src="/images/authentication-flow/web-app/mobile/general-passkey-management-android.png" alt="Web app mobile passkey management flow on Android" />
    </Frame>
  </Tab>
</Tabs>

<Steps>
    <Step title="General Passkey Settings in Profile Section">
        - Users can see all the passkeys created via current account here and
        can initiate passkey registration for new devices or update existing
        credentials.
    </Step>
    <Step title="When No Passkey is Created for the User Account">
        - When user has no passkey linked, passkey list is shown empty.
    </Step>
    <Step title="Passkey Delete Modal">
        - User can simply delete existing passkeys from the list shown by
        clicking on the "X" icon on the top right corner of any passkey listed.
        - This action is not reversible, so the user is again asked for
        confirmation on deletion of passkey. This leads to permanent passkey
        deletion. - User has the ability to create a new one.
    </Step>
</Steps>

### Passkey List Error States

<Tabs>
  <Tab title="macOS">
    <Frame caption="macOS passkey list error states">
      <img src="/images/authentication-flow/web-app/desktop/passkey-list-errors-macos.png" alt="Web app desktop passkey list error states on macOS" />
    </Frame>
  </Tab>

<Tab title="Windows">
    <Frame caption="Windows passkey list error states">
        <img
            src="/images/authentication-flow/web-app/desktop/passkey-list-errors-windows.png"
            alt="Web app desktop passkey list error states on Windows"
        />
    </Frame>
</Tab>
<Tab title="iOS">
    <Frame caption="iOS passkey list error states">
        <img
            src="/images/authentication-flow/web-app/mobile/passkey-list-errors-ios.png"
            alt="Web app mobile passkey list error states on iOS"
        />
    </Frame>
</Tab>

  <Tab title="Android">
    <Frame caption="Android passkey list error states">
      <img src="/images/authentication-flow/web-app/mobile/passkey-list-errors-android.png" alt="Web app mobile passkey list error states on Android" />
    </Frame>
  </Tab>
</Tabs>

<Steps>
    <Step title="Passkey List Loading Error">
        - The list of passkeys fails to load (e.g., due to network or system
        error).
    </Step>
    <Step title="User Aborts Passkey Creation">
        - User cancels creation during system prompts or internal dialogs. No
        new passkey is added in the list.
    </Step>
    <Step title="Passkey Creation Failed Due to API Errors">
        - User experiences errors from backend/API during passkey creation. The
        error is communicated clearly.
    </Step>
    <Step title="Passkey Deletion Fails">
        - Deletion of the passkey returns an error due to system or API failure.
    </Step>
    <Step title="Passkeys Already Exists">
        - Attempting to register a passkey that is already present prompts an
        informative error.
    </Step>
    <Step title="No Passkey Support">
        - Device or browser does not support passkeys, blocking registration or
        management.
    </Step>
</Steps>
