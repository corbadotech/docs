---
title: "Conditional UI"
description: "This flow details how users sign in when passkeys are suggested based on context or user action."
---

import { ToolingFrame } from "/snippets/authentication-flow/tooling-frame.mdx";

## Autosuggested Passkeys

Passkeys can appear as suggestions directly on the keyboard’s suggestion bar, allowing users to select and log in quickly without opening an extra prompt.

<Tabs>
  <Tab title="macOS">
    <ToolingFrame 
        imageURL="/images/authentication-flow/web-app/desktop/autosuggest-passkeys-macos.png"
        caption="macOS autosuggested passkey flow"
        imageName="autosuggest-passkeys-macos.png"
    />
  </Tab>

<Tab title="Windows">
    <ToolingFrame
        imageURL="/images/authentication-flow/web-app/desktop/autosuggest-passkeys-windows.png"
        caption="Windows autosuggested passkey flow"
        imageName="autosuggest-passkeys-windows.png"
    />
</Tab>

<Tab title="iOS">
    <ToolingFrame
        imageURL="/images/authentication-flow/web-app/mobile/autosuggest-passkeys-ios.png"
        caption="iOS autosuggested passkey flow"
        imageName="autosuggest-passkeys-ios.png"
    />
</Tab>

  <Tab title="Android">
    <ToolingFrame 
        imageURL="/images/authentication-flow/web-app/mobile/autosuggest-passkeys-android.png"
        caption="Android autosuggested passkey flow"
        imageName="autosuggest-passkeys-android.png"
    />
  </Tab>
</Tabs>

<Steps>
    <Step title="Passkey auto-suggestion appears in keyboard">
        - When the user taps on the email field, an autosuggested passkey appears on the keyboard based on the stored credential for that domain.
    </Step>
     <Step title="Authenticate using the selected passkey">
        - The user taps the suggested passkey on the keyboard to continue with authentication.
        - User verify their identity using their screen lock (e.g., fingerprint, PIN, or face recognition).
    </Step>
     <Step title="Access granted">
        - User is seamlessly logged in and redirected to the dashboard.
    </Step>

</Steps>
