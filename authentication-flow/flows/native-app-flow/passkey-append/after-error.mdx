---
title: "Passkey Append: After Error"
description: "This flow handles cases where the initial attempt to create or register a passkey did not succeed due to system errors, user cancellations, or other interruptions. It ensures that the user can retry appending a passkey securely without losing access and with clear guidance to resolve errors."
---

## Passkey Creation After Error

Users logging in native app, follows a standard hybrid login flow and experience passkey append after error.

<Tabs>
<Tab title="iOS">
    <Frame caption="Native: iOS passkey append after error">
        <img
            src="/images/authentication-flow/native-app/append-after-error-ios-native.png"
            alt="Native app passkey append after error on iOS"
        />
    </Frame>
</Tab>

  <Tab title="Android">
    <Frame caption="Native: Android passkey append after error">
      <img src="/images/authentication-flow/native-app/append-after-error-android-native.png" alt="Native app passkey append after error on Android" />
    </Frame>
  </Tab>
</Tabs>

<Info>
    NOTE: The blue background indicates product-native steps (e.g., verification
    code, setup PIN, enabling biometrics). Passkey creation is seamlessly
    embedded in your product journey.
</Info>
<Steps>
    <Step title="Login when user has no passkeys">
        - User enters their identifier (email/username) on the login page.
    </Step>
    <Step title="Fallback to traditional login method">
        - System detects no passkey created for the respective account, system fallbacks to traditional login method (username + password).
    </Step>
    <Step title="Passkey append process initiates">
        - User is offered an option to create a passkey for future logins.
    </Step>
    <Step title="Passkey append process abruptly cancels or an error occurs">
        - There is an abrupt error faced during passkey creation process, the user is not negatively notified for the error. Instead, an informing screen appears where the user is again asked to create a passkey.
    </Step>
    <Step title="OS-native biometric modal appears">
        - User continue to set up a passkey using fingerprint, face, or PIN for secure sign-in.
    </Step>
    <Step title="Passkey creation successful">
        - User successfully creates passkey and is now linked with the user account for the respective web-app.
    </Step>
     <Step title="App PIN setup">
        - After successful passkey creation, the user is signed in and is asked
        to setup PIN.
    </Step>
    <Step title="Enable local biometric for app login">
        - User is allowed to enable/disable local biometric app login (provided
        as product feature).
    </Step>

</Steps>
