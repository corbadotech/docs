---
title: "Conditional UI"
description: "Using conditional UI for passkey login in native apps."
---

import { ToolingFrame } from "/snippets/authentication-flow/tooling-frame.mdx";

## Autosuggested Passkeys

Passkeys can appear as suggestions directly on the keyboard’s suggestion bar, allowing users to select and log in quickly without opening an extra prompt.

<Tabs>
    <Tab title="iOS">
        <ToolingFrame
            imageURL="/images/authentication-flow/native-app/autosuggested-passkeys-ios-native.png"
            caption="Native: iOS autosuggested passkey flow"
            imageName="autosuggested-passkeys-ios-native.png"
        />
    </Tab>
</Tabs>

<Info>
    NOTE: The blue background indicates product-native steps (e.g., verification
    code, setup PIN, enabling biometrics). Passkey creation is seamlessly
    embedded in your product journey.
</Info>

<Steps>
    <Step title="Passkey auto-suggestion appears in keyboard">
        - When the user taps on the email field, an autosuggested passkey appears on the keyboard based on the stored credential for that domain.
    </Step>
     <Step title="Authenticate using the selected passkey">
        - The user taps the suggested passkey on the keyboard to continue with authentication.
    </Step>
     <Step title="Device verifies user">
        - User verify their identity using their screen lock (e.g., fingerprint, PIN, or face recognition).
    </Step>
     <Step title="App PIN setup">
        - After successful passkey creation, the user is signed in and is asked
        to setup PIN.
    </Step>
    <Step title="Enable local biometric for app login">
        - User is allowed to enable/disable local biometric app login (provided
        as product feature).
    </Step>

</Steps>
