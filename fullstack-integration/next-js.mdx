---
title: "Setup"
description: "Learn how to add the Corbado React package to your Next.js application."
---

import Installation from "/snippets/react-prerequisites.mdx";

<Note>
    The integration process is slightly different for the Next.js pages router.
    Make sure to have a look at our [dedicated page](/frontend-integration/next-js/pages-router), if you're using the pages router.
</Note>

## Installation

<Installation />

## Setup Corbado

Create a wrapper around the `CorbadoProvider`, to render it on the client:

```tsx ProviderWrapper.tsx
"use client";

import { CorbadoProvider } from "@corbado/react";

export default function ProviderWrapper({
    children
}: {
    children: React.ReactNode;
}) {
    return (
        <CorbadoProvider projectId="your-project-id">
            {children}
        </CorbadoProvider>
    );
}
```

Now, integrate the `ProviderWrapper` into your root layout:

```tsx layout.tsx
import ProviderWrapper from "./ProviderWrapper";

export default function RootLayout({
    children
}: {
    children: React.ReactNode;
}) {
    return (
        <html lang="en">
            <ProviderWrapper>
                <body>{children}</body>
            </ProviderWrapper>
        </html>
    );
}
```

<Note>
    Most customization is done in the `CorbadoProvider`.
    See a full list of options for the `CorbadoProvider` in the [CorbadoProvider documentation](/ui-components/corbadoprovider).
</Note>

## Next

1. Add [authentication UI components](/frontend-integration/next-js/authentication-ui) to your application.
2. Let your users manage their passkeys with the [PasskeyList](/frontend-integration/next-js/passkeylist).
3. Learn how you can access the users authentication state on the [client-side](/frontend-integration/next-js/authentication-state-client).
4. Easily [access user information on the server-side](/frontend-integration/next-js/authentication-state-server)
with the [Node.js SDK](/backend-integration/node-js-sdk).
5. If you're using the pages router, have a look at our [dedicated page](/frontend-integration/next-js/pages-router).